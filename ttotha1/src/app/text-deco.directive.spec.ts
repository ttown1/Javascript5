import { TextDecoDirective } from './text-deco.directive';

describe('TextDecoDirective', () => {
  it('should create an instance', () => {
    const directive = new TextDecoDirective();
    expect(directive).toBeTruthy();
  });
});
